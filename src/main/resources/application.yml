spring:
  application:
    name: microservice-nacos
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_SERVER_ADDR}
        file-extension: yaml
        group: DEFAULT_GROUP
        import-check:
           enabled: false
